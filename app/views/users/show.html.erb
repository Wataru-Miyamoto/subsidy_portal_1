<% provide(:title, @user.name) %>
<h2><%= @user.name %></h2>
<div>
  <table class="table table-bordered table-condensed user-table">
    <tr>
      <td>ユーザーネーム</td>
      <td><%= @user.name %></td>
    </tr>

    <tr>
      <td>メールアドレス</td>
      <td><%= @user.email %></td>
    </tr>

    <tr>
      <td>ユーザーID</td>
      <td><%= @user.id %></td>
    </tr>
  </table>
</div>
