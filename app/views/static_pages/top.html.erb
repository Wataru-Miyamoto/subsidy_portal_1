<% provide(:title, "Top") %>

<div class="center jumbotron text-center">
  <h1>補助金・助成金・減免検索ツール</h1>
  <% unless logged_in? %>
    <%= link_to "ログイン", login_path, class: "btn btn-lg btn-info" %>
    <p>
      アカウント作成は<%= link_to "こちら", signup_path %>から
    </p>
  <% else %>
    <h3><%= current_user.name %>さん！　ようこそ！</h3>
  <% end %>

</div>
<div class="center text-center">
<%= link_to "都道府県一覧を見る", prefs_path, method: :get, class: "btn btn-lg btn-primary" %>
</div>
